name: Release Desktop Builds

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: 22
  CSC_IDENTITY_AUTO_DISCOVERY: "false"

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Tests
        run: npm test

      - name: Verify tag matches package version
        env:
          TAG: ${{ github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          test "v$VERSION" = "$TAG" || {
            echo "Tag $TAG does not match package.json version $VERSION" >&2
            exit 1
          }

  windows:
    name: Windows (.exe)
    needs: validate
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build standalone application
        env:
          DATABASE_URL: "file:./dev.db"
        run: npm run build:standalone

      - name: Package Windows installer and portable build
        run: npx electron-builder --win nsis portable --x64 --publish never

      - name: Publish Windows assets to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: gh release upload "${{ github.ref_name }}" release/*.exe release/latest.yml --clobber

      - name: Keep short-lived Windows build diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: release-windows
          path: |
            release/*.exe
            release/latest.yml
          if-no-files-found: error
          retention-days: 1

  macos:
    name: macOS (Universal .dmg/.zip)
    needs: validate
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build standalone application
        env:
          DATABASE_URL: "file:./dev.db"
        run: npm run build:standalone

      - name: Package universal macOS builds
        run: npx electron-builder --mac dmg zip --universal --publish never

      - name: Publish macOS assets to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: gh release upload "${{ github.ref_name }}" release/*.dmg release/*.zip release/latest-mac.yml --clobber

      - name: Keep short-lived macOS build diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: release-macos
          path: |
            release/*.dmg
            release/*.zip
            release/latest-mac.yml
          if-no-files-found: error
          retention-days: 1

  linux:
    name: Linux (.AppImage/.deb)
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build standalone application
        env:
          DATABASE_URL: "file:./dev.db"
        run: npm run build:standalone

      - name: Package Linux builds
        run: npx electron-builder --linux AppImage deb --x64 --publish never

      - name: Publish Linux assets to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: gh release upload "${{ github.ref_name }}" release/*.AppImage release/*.deb release/latest-linux.yml --clobber

      - name: Keep short-lived Linux build diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: release-linux
          path: |
            release/*.AppImage
            release/*.deb
            release/latest-linux.yml
          if-no-files-found: error
          retention-days: 1
